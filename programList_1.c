#include<stdio.h>
	int main(void)
	{  
	  float weight, zone, price;
	  scanf("%f,%f", zone, weight);
	  
	  if (zone < 0 && zone > 4)
	  {
		printf("Error in Area\n");
	  }
	  
	  if (weight < 0)
	  {
		printf("Error in Area\n");
	  }
	  
	  if (zone = 0)
	  {
		
	  }
	  

       return 0;
	}
